# ─────────────────────────────────────────────────────────────────────────────
# Lumen Pipeline Server
# ─────────────────────────────────────────────────────────────────────────────
# For LOCAL DEVELOPMENT only. For production, use `lets deploy-frontend`
# which dynamically injects the Cloud Run URL and API key from Secret Manager.
# ─────────────────────────────────────────────────────────────────────────────

# Cloud Run backend URL — used for server-side 3D shape generation.
# For local dev: keep as localhost (run the backend with `lets serve`).
# For production: `lets deploy-frontend` overrides this automatically.
VITE_LUMEN_SERVER_URL=https://lumen-pipeline-149036788105.us-central1.run.app
VITE_LUMEN_API_KEY=your-api-key-here

# Tier 2 motion plan server (A5 LLM inference)
VITE_MOTION_SERVER_URL=http://localhost:8080

# Deepgram API key for WebSocket STT fallback (when Web Speech API unavailable)
# Get a key at https://console.deepgram.com — $200 free credit
VITE_DEEPGRAM_API_KEY=your-deepgram-api-key-here
